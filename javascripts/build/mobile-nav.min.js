!function($) {
    $(document).ready(function() {
        var $menuContainer = $("#mainMenuContainer"), $menuToggle = $("#mobileMenuToggle", $menuContainer), $menu = $("#mainMenu", $menuContainer), $menuItemsWithChildren = $(".has-children", $menuContainer);
        $menuToggle.on("click", function(e) {
            var hidden = "true" == $menu.attr("aria-hidden") ? "false" : "true";
            e.preventDefault(), $menuContainer.toggleClass("is-revealed"), $menu.attr("aria-hidden", hidden);
        }), $menuItemsWithChildren.each(function(i) {
            var $toggler = $('<span class="js-toggle-children accordion-nav__toggle-children"><i class="icon plus-to-minus"></i></span>');
            $(this).find("> a").after($toggler);
        });
        var $togglers = $(".js-toggle-children");
        $togglers.each(function(i) {
            var $thisToggler = $($togglers[i]);
            $(this).on("click", function(e) {
                e.preventDefault();
                var hidden = "true" == $thisToggler.siblings("ul").attr("aria-hidden") ? "false" : "true";
                $thisToggler.parent("li").toggleClass("is-open"), $thisToggler.siblings("ul").attr("aria-hidden", hidden);
            });
        });
    });
}(jQuery);