!function(window, $, PROBO) {
    if (List instanceof Function) {
        var options = {
            valueNames: [ "name", "recipe", "highlight", "s", "no" ]
        };
        new List("recipes", options);
    }
    $(document).ready(function(e) {
        var $menuContainer = $("#mainMenuContainer"), $menuToggle = $("#mobileMenuToggle", $menuContainer), $menu = $("#mainMenu", $menuContainer), $menuItemsWithChildren = $(".has-children"), $overlay = $("#overlay", $menuContainer), mobileMenu = new PROBO.MobileMenu($menuContainer, $menuToggle, $menu, $menuItemsWithChildren, $overlay);
        $menuToggle.on("click", function(e) {
            e.preventDefault(), mobileMenu.menuIsOpen ? mobileMenu.closeMenu() : mobileMenu.openMenu();
        }), $overlay.on("click", function(e) {
            e.preventDefault(), mobileMenu.menuIsOpen ? mobileMenu.closeMenu() : mobileMenu.openMenu();
        });
    });
}(window || {}, jQuery, PROBO);