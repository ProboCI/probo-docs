!function(window, $) {
    if ($(document).ready(function() {
        $.fn.tinyNav instanceof Function && $("#sidebar-first .accordion-nav").tinyNav(), 
        $.fn.sidr instanceof Function && ($("#simple-menu").css("display", "").sidr({
            side: "right"
        }), $(window).on("resize", function(event) {
            $("body").hasClass("sidr-open") && $(window).width() >= 768 && $.sidr("close");
        })), $(".accordion-nav").navgoco({
            caretHtml: '<i class="icon plus-to-minus"></i>',
            accordion: !0,
            openClass: "open",
            save: !0,
            cookie: {
                name: "navgoco",
                expires: !1,
                path: "/"
            }
        });
    }), List instanceof Function) {
        var options = {
            valueNames: [ "name", "recipe", "highlight", "s", "no" ]
        };
        new List("recipes", options);
    }
}(window || {}, jQuery);